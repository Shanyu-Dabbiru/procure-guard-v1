[project]
name = "procure-guard"
version = "0.1.0"
description = "Autonomous Procurement Guardrail (APG)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.0.25",
    "pydantic>=2.0.0",
    "qdrant-client>=1.7.0",
    "logfire[pydantic]>=0.4.0",
    "langchain>=0.1.0",
    "langchain-core>=0.1.0",
    "langchain-openai>=0.0.2"
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.2.0"
]
